@model KermesseApp.Models.tbl_moneda

@{
    ViewBag.Title = "Moneda";
}

<div class="card border-secondary formulario">
    @using (Html.BeginForm("GuardarMon", "Tbl_moneda", FormMethod.Post))
    {
        <div class="card-header">
            <h2>Nueva Moneda</h2>
        </div>

        <div class="card-body form-horizontal container-fluid">

            <div class="form-group row">
                @Html.LabelFor(x => x.nombre, htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="col-md-10">
                    @Html.TextBoxFor(x => x.nombre, new { @class = "form-control" })
                    @Html.ValidationMessageFor(x => x.nombre, "", new { @class = "text-danger" })
                </div>
            </div>

            <div class="form-group row">
                @Html.LabelFor(x => x.signo, htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="col-md-10">

                    @Html.TextAreaFor(x => x.signo, new { @class = "form-control" })
                    @Html.ValidationMessageFor(x => x.signo, "", new { @class = "text-danger" })
                </div>
            </div>

            <br />

            <div class="form-group row justify-content-center">
                <input type="submit" value="Guardar" class="btn btn-success col-md-2" />
                <input type="reset" value="Cancelar" class="btn btn-danger col-md-2" />
            </div>
        </div>
    }
</div>

<div>
    @Html.ActionLink("Regresar", "tbl_Moneda")
</div>

