@model IEnumerable<KermesseApp.Models.tbl_rol_opciones>

@{
    ViewBag.Title = "Tbl_rolopciones";
}

<h2>Tbl_rolopciones</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tbl_opciones.opcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tbl_rol.rol_desc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tbl_opciones.opcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tbl_rol.rol_desc)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_rol_opcion }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_rol_opcion }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_rol_opcion })
        </td>
    </tr>
}

</table>
