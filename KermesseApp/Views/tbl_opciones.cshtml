@model IEnumerable<KermesseApp.Models.tbl_opciones>

@{
    ViewBag.Title = "tbl_opciones";
}

<h2>tbl_opciones</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.opcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.estado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.opcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.estado)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_opciones }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_opciones }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_opciones })
        </td>
    </tr>
}

</table>
